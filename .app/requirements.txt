# Claude Session Linker GUI dependencies
# Recommended (isolated, self-contained): powershell -File .app\setup.ps1
# Manual venv: python -m venv .app\venv && .app\venv\Scripts\python -m pip install -r .app\requirements.txt
customtkinter==6.0.0
darkdetect==0.8.0
pillow==12.2.0
